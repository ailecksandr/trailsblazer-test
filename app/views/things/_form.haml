= form_for form do |f|
    = f.label :name
    = f.text_field :name, required: true, disabled: form.readonly?(:name)

    = f.label :description
    = f.text_field :description, required: true

    = f.submit 'Create'
    = link_to 'Cancel', things_path

= form.errors.messages
